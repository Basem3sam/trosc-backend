# Testing Guide (Jest + Supertest)

A practical guide to the concepts behind the test setup in this repo, written for someone who hasn't used Jest or Supertest before — and a complete map of what's actually covered today.

> 📌 This file is regenerated by hand whenever the test suite changes meaningfully. If you add a test file, add a row to the [coverage table](#whats-covered-so-far) below in the same PR.

---

## The two tools, in plain terms

**Jest** is the _test runner and assertion library_. It's what gives you `describe`, `it`, and `expect`, runs your test files, and tells you what passed/failed. You never `require('jest')` in your code — Jest injects those functions globally when it runs your files.

**Supertest** is a library that lets you send fake HTTP requests directly at your Express `app` object — no real server, no real port, no actual network call. It's what gives you `request(app).post('/v1/contact').send({...})`. Under the hood it spins up your Express app on a random ephemeral port for the duration of that one request, then tears it down — you never have to call `app.listen()` yourself in tests.

Together: Jest runs the test and checks results, Supertest is _how you make the request_ that produces those results. Not every test file uses Supertest — the `services/` and `utils/` suites call service/utility functions directly instead, since there's no HTTP layer to exercise for that kind of test.

## Why `server.js` and `src/app.js` being separate matters

This project already has the right shape for testing, which is why none of this required restructuring your code:

- `src/app.js` builds the Express app and does `module.exports = app` — no `.listen()` call.
- `server.js` is the only place that calls `app.listen()`, and it's also the only place that calls `db.config.js` to connect to a real MongoDB.

That separation means a test file can `require('../src/app')` and get a fully-configured Express app — routes, middleware, error handling, all of it — without starting a real server or touching your real database. If `app.listen()` lived inside `app.js`, importing it for tests would start a real server as a side effect every time, which is exactly what you don't want.

## The moving pieces in this repo

```
jest.config.js               — tells Jest where tests live and what setup to run
.env.test                    — test-only env vars (fake JWT secret, generous rate limits)
tests/
  globalSetup.js              — runs ONCE before any test file: starts an in-memory
                                 MongoDB *replica set*, loads .env.test
  globalTeardown.js           — runs ONCE after all tests finish: stops that instance
  setupAfterEnv.js             — runs before/after each test FILE: connects Mongoose,
                                 auto-mocks src/utils/Email, wipes collections between tests
  __mocks__/
    Email.js                  — manual mock for src/utils/Email, activated globally by
                                 setupAfterEnv.js's jest.mock('../src/utils/Email')
  helpers/
    testUser.js                — creates a user + valid JWT without hitting /signup or
                                 /login, skipping the rate limiter entirely
    fixtures.js                 — shared track/course/session fixture builders, used by
                                 reviews.test.js and assignments.test.js
  config/
    mailer.config.test.js       — unit tests for the Nodemailer transporter factory
  controllers/
    course.controller.test.js    — course endpoints not covered elsewhere (session
                                 add/remove, filtering routes)
    session.controller.test.js   — same, for sessions
    track.controller.test.js     — analytics, pending students/leaves, approve/reject
    weeklyTask.controller.test.js — top-level update/delete of a weekly task bucket
  services/
    course.service.test.js       — course service unit tests
    enrollment.service.test.js   — enrollment/leave/approve/reject/prerequisite logic
    session.service.test.js      — session service unit tests
    track.service.test.js        — track deletion cascade + edge cases
    weeklyTask.service.test.js   — weekly task CRUD + completion-tracking service logic
    activityLog.service.test.js  — logActivity write path, all read ops, summary aggregation, prune
    dashboardStats.service.test.js — period-boundary math, snapshot computation/upsert, trends, prune
  utils/
    attachmentValidation.test.js — Joi-level attachment URL validator
    Email.test.js                — the real (unmocked-in-this-file) Email class
    escapeHtml.test.js           — HTML-escaping helper
    logger.test.js               — Winston logger config + request-ID injection
  adminUsers.test.js             — admin user list/create/get/update/delete
  announcement.test.js           — announcement CRUD + role gating
  APIFeatures.test.js             — the filter/sort/paginate/search query-builder class
  app.test.js                     — src/app.js itself: prod vs dev config, CORS, 404,
                                 global rate limiter, Swagger UI gating, /health
  activityLog.test.js             — activity-log routes: auth/admin guards, /me,
                                 admin list/user/summary/delete/prune, real
                                 signup/login/updateMe actions actually get logged
  assignments.test.js             — assignment CRUD, listing, submission, resubmission,
                                 grading
  auth.test.js                    — real /signup and /login: happy paths, role-injection
                                 rejection, password hashing, duplicate email, user
                                 enumeration protection, deactivated accounts
  bulkUser.test.js                — admin bulk activate/deactivate/delete
  cascade.test.js                 — cascade.service.js sync/unsync functions directly
                                 (runs against a real replica set — see below)
  contact.test.js                 — public contact form: submission + validation
  dashboardStats.test.js          — dashboard-stats routes: admin-only guards on every
                                 route, live stats, snapshot generation/upsert, latest,
                                 trends, list/filter, get/delete by id, prune
  contactAdmin.test.js            — admin contact triage: list, view, update status
  enrollment.test.js              — self-enroll/approve/reject/leave over HTTP, course
                                 & session access rules, prerequisites
  error.controller.test.js        — the global error handler in isolation (dev vs prod
                                 payloads, CastError/duplicate-key/ValidationError/JWT
                                 error mapping)
  errorHandling.test.js           — the same error paths exercised through a real route
  events.test.js                  — event CRUD + RSVP
  feed.test.js                    — dashboard feed aggregation
  passwordReset.test.js           — forgotPassword / resetPassword / updateMyPassword
  reviews.test.js                 — track/course/session review creation, listing,
                                 deletion
  sessionGating.test.js           — url/embedUrl/resources stripped for non-enrolled users
  trackCourseCRUD.test.js         — track & course CRUD happy paths
  trackEndpoints.test.js          — /popular, /analytics, /pending, leave requests,
                                 /student/:studentId
  updateMe.test.js                — base64-photo regression + enrolledTrack presence
  validateAttachments.test.js     — the Mongoose-level attachment URL validator
  weeklyTask.test.js              — course-scoped weekly task creation + completion
                                 tracking
```

### Why an in-memory MongoDB **replica set** instead of your real dev database?

`mongodb-memory-server` downloads a real MongoDB binary (once) and runs a real, throwaway MongoDB instance in memory for the duration of your test run. This matters for three reasons:

1. **Isolation** — tests can create/delete/mutate data freely without ever touching your actual dev data.
2. **Speed** — no network latency to Atlas; everything is local.
3. **Repeatability** — every test run starts from a genuinely empty database, so a test can never accidentally pass (or fail) because of leftover data from a previous run or another developer's local database.

`globalSetup.js` specifically starts a **`MongoMemoryReplSet`** (a single-node replica set), not a plain standalone instance. This is deliberate: MongoDB transactions — used in `cascade.service.js` for atomic enrollment sync — only work against a replica set. `cascade.test.js` exercises `syncUserEnrollments`, `unsyncUserEnrollments`, `syncCourseEnrollment`, `unsyncCourseEnrollment`, `syncSessionEnrollment`, and `unsyncSessionEnrollment` directly against this instance, so the transaction paths are genuinely covered.

The first time you run tests, it'll download the MongoDB binary — that needs an internet connection and takes a few seconds. After that it's cached locally and startup is fast.

### Email is mocked globally

`setupAfterEnv.js` calls `jest.mock('../src/utils/Email')` once, before any test file runs. Jest resolves that to the manual mock at `tests/__mocks__/Email.js`, whose methods (`sendWelcome`, `sendPasswordReset`, `sendEnrollmentConfirmation`, `sendSessionReminder`, `send`) are all no-ops. This applies to **every** test file automatically — no individual test needs its own `jest.mock('../../src/utils/Email')` call, and no test run ever hits a real SMTP server. `tests/utils/Email.test.js` is the one place that tests the real `Email` class's behavior; it does so by mocking `Email`'s own dependencies (`mailer.config`, `logger`, `html-to-text`) instead, one level lower than the rest of the suite.

## Anatomy of one test

```js
describe('POST /v1/contact', () => {
  it('accepts a valid submission and stores it', async () => {
    const res = await request(app).post('/v1/contact').send(validSubmission);

    expect(res.status).toBe(200);
    expect(res.body.status).toBe('success');
  });
});
```

- `describe(...)` — groups related tests together. Purely organizational, shows up in the test output as a heading.
- `it(...)` (or its alias `test(...)`) — one actual test case. The string describes what should be true; the function body proves it.
- `request(app).post(...)` — Supertest builds an HTTP request against your app.
- `.send(...)` — attaches a JSON body (Supertest sets the right headers automatically).
- `expect(res.status).toBe(200)` — a Jest **assertion**. If `res.status` isn't `200`, the test fails right here and Jest reports it.

Common Jest matchers you'll use constantly: `.toBe(x)` (exact equality), `.toEqual(x)` (deep equality for objects/arrays), `.toBeNull()`, `.toHaveLength(n)`, `.not.toBe(x)`.

## Testing an authenticated route

Real signup goes through email verification, rate limiting, and password hashing — all real behavior you don't want to fight with in every single test that isn't actually _about_ auth. `tests/helpers/testUser.js` shortcuts this:

```js
const { createTestUser } = require('./helpers/testUser');

const { user, token } = await createTestUser({ role: 'instructor' });

const res = await request(app)
  .post(`/v1/courses/${course._id}/weekly-tasks`)
  .set('Authorization', `Bearer ${token}`) // <-- this is the key part
  .send({ week: 1, title: 'Week 1', items: [...] });
```

It calls `User.create()` directly (so no signup endpoint, no rate limiter, no email sent) and signs a real JWT with your actual `generateToken` util — so the token is indistinguishable from one a real login would produce. `.set('Authorization', ...)` attaches it exactly like a real client would.

`auth.test.js` is the one file that deliberately does the opposite — it hits `/v1/users/signup` and `/v1/users/login` directly through Supertest, because that endpoint behavior is exactly what it's testing.

## Why fixtures are built with `Model.create()`, not through the API

`reviews.test.js`, `assignments.test.js`, `weeklyTask.test.js`, and most of the `services/` and `controllers/` suites all build track/course/session setup directly via `Track.create()` / `Course.create()` / `Session.create()` rather than hitting the real creation endpoints. This is a deliberate and common pattern:

- **Speed** — no need to run through every layer of validation/middleware just to get data into place for a test that isn't _about_ track/course creation.
- **Focus** — if this test fails, you know it's about reviews/assignments/weekly-tasks/etc., not accidentally exposing a bug in course creation.

The trade-off: those tests don't verify that track/course/session creation _itself_ works — that's what `trackCourseCRUD.test.js` is for, testing that flow through the real endpoints.

### `tests/helpers/fixtures.js`

`reviews.test.js` and `assignments.test.js` share one fixture builder (`buildTrackAndCourseFixture`, `buildStandaloneSessionFixture`) instead of each defining their own — both needed the exact same shape (a track, a course inside it, an owning instructor, an unrelated instructor for ownership-rejection tests, and an enrolled student). `weeklyTask.test.js` still has its own local copy of a similar fixture, written before this shared helper existed; it works fine as-is, so it wasn't worth touching a passing file just for consistency's sake. If you add another test file needing the same shape, use the shared one rather than copy-pasting again.

## Two flavors of test in this suite

Most files are **integration tests** — they go through Supertest and exercise real routes, middleware, validation, and the database together. A smaller set are **unit tests** that skip HTTP entirely:

- `services/*.test.js` call service functions (`enrollmentService.enrollInCourse(...)`, `trackService.deleteTrack(...)`, etc.) directly, still against the real in-memory database, but without going through Express/Joi/ownership middleware.
- `utils/*.test.js` and `APIFeatures.test.js` test pure functions/classes with no database or HTTP involved at all (`escapeHtml`, `attachmentValidation`, `Email`, `logger`, `APIFeatures`).
- `config/mailer.config.test.js` tests the Nodemailer transporter factory with `nodemailer` itself mocked.
- `error.controller.test.js` builds a throwaway Express app around just the error handler, so it can assert on dev-vs-prod error payloads without any other middleware in the way.
- `app.test.js` tests `src/app.js` configuration behavior (trust proxy, CORS, Swagger gating, the global rate limiter, `/health`) by toggling `NODE_ENV` and re-requiring the app module.

Both flavors matter: the unit-style tests are faster and pinpoint exactly which layer broke; the integration-style tests are what actually prove an endpoint works end-to-end.

## Running the tests

```bash
npm test              # run once
npm run test:watch    # re-run automatically as you edit files
npm run test:coverage # run once + generate a coverage report
```

`npm test` runs with `--runInBand`, meaning test files run one after another rather than in parallel. This project's tests share one in-memory MongoDB instance, so running them one at a time avoids test files stepping on each other's data mid-run. (Individual tests _within_ one file still run fast — this only serializes across files.)

### Running a specific test file

```bash
npm test -- tests/contact.test.js
npm test -- tests/services/enrollment.service.test.js
```

Or if you want to watch only that file:

```bash
npm run test:watch -- tests/contact.test.js
```

### Test coverage

```bash
npm run test:coverage
```

Coverage reports are generated in `coverage/lcov-report/index.html`. Open it in your browser to see which lines are covered. You can set a minimum coverage threshold by adding `coverageThreshold` to `jest.config.js`.

### Continuous Integration

`.github/workflows/test.yml` runs `npm test` on every push via GitHub Actions (Node 18, `NODE_ENV=test`, with generous rate-limit overrides so auth-heavy tests don't trip the real 5-per-15-minutes limit). There's no separate CI env file to keep in sync manually — the workflow sets the handful of variables the suite actually needs (`JWT_SECRET`, `JWT_EXPIRES_IN`, `JWT_COOKIE_EXPIRES_IN`, `FRONTEND_URL`, `AUTH_RATE_LIMIT_MAX`, `AUTH_RATE_LIMIT_WINDOW_MS`, `RATE_LIMIT_MAX`, `RATE_LIMIT_WINDOW_MS`) directly as job-level env vars, mirroring `.env.test`.

## Debugging tests

### Using Node inspector

```bash
node --inspect-brk node_modules/.bin/jest --runInBand tests/your-test.test.js
```

Then open `chrome://inspect` in Chrome and attach the debugger.

### Using `console.log`

Jest captures `console.log` output and shows it only for failed tests. You can also use `--verbose` to see all logs (the project's `jest.config.js` already sets `verbose: true`, so per-test names print by default).

### Using the debugger statement

Add `debugger;` in your test or code, then run:

```bash
npm test -- --inspect-brk
```

## What's covered so far

| File                                        | Covers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `auth.test.js`                              | Signup (happy path, password hashing, role-injection rejection, duplicate email, validation errors) and login (happy path, `lastLogin` update, wrong password, unknown email, identical error message for both to confirm no user enumeration, deactivated account, missing fields), plus logout                                                                                                                                                                                                                                                  |
| `passwordReset.test.js`                     | `forgotPassword` (token issuance, no email-enumeration leak) and `resetPassword` (valid/invalid token), and `updateMyPassword` (correct vs. wrong current password)                                                                                                                                                                                                                                                                                                                                                                               |
| `adminUsers.test.js`                        | Admin `GET/POST /v1/users`, `GET/PATCH/DELETE /v1/users/:id`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `bulkUser.test.js`                          | Admin bulk activate/deactivate/delete, self-targeting and other-admin-targeting rejection                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `trackCourseCRUD.test.js`                   | Track create/read/update/delete and course create/add-session/add-remove-student/delete happy paths                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `trackEndpoints.test.js`                    | `/v1/tracks/popular`, `/:id/analytics` (role-gated), `/:id/pending` (role-gated), leave-request GET/approve/reject (including self-approval block), `/v1/tracks/student/:studentId` (self vs. others)                                                                                                                                                                                                                                                                                                                                             |
| `controllers/track.controller.test.js`      | Analytics, pending-students list, pending-leaves list, and approve/reject for both students and leave requests — ownership gating, self-approval block, 404s, role gating                                                                                                                                                                                                                                                                                                                                                                         |
| `controllers/course.controller.test.js`     | Add/remove session to/from a course, add/remove student to/from a course, and the `instructor/:id` / `track/:id` / `student/:id` filtering routes — all with ownership and role-rejection cases                                                                                                                                                                                                                                                                                                                                                   |
| `controllers/session.controller.test.js`    | Add/remove student to/from a session, and the `instructor/:id` / `track/:id` / `student/:id` filtering routes                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `controllers/weeklyTask.controller.test.js` | `PATCH`/`DELETE /v1/weekly-tasks/:taskId` — ownership, admin bypass, role gating, week-clash rejection, empty-body rejection, 404                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `sessionGating.test.js`                     | `url`/`embedUrl`/`resources` stripped for non-enrolled callers, present for enrolled students                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `enrollment.test.js`                        | Track self-enroll/approve/reject/leave over HTTP; course access rules (public/track-only/private) and prerequisites; session self-enrollment rules                                                                                                                                                                                                                                                                                                                                                                                                |
| `services/enrollment.service.test.js`       | Every enrollment/leave/approve/reject/prerequisite code path in `enrollment.service.js` directly, including edge cases not reachable (or not yet exercised) via HTTP                                                                                                                                                                                                                                                                                                                                                                              |
| `cascade.test.js`                           | `cascade.service.js`'s sync/unsync functions directly, against a real replica set — the actual MongoDB transaction paths                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `services/track.service.test.js`            | `deleteTrack` cascade (assignments, reviews, weekly tasks removed; courses orphaned; sessions updated), 404, and the "can't empty a track via update" edge case                                                                                                                                                                                                                                                                                                                                                                                   |
| `services/course.service.test.js`           | `createCourse`, `getAllCourses`, `getCourseById` (with/without populated sessions), `getCourseDetails` (student visibility rules), `updateCourse`, `deleteCourse` (cascade), session add/remove, student enroll/remove, and the instructor/track/student filter queries                                                                                                                                                                                                                                                                           |
| `services/session.service.test.js`          | `createSession`, `getAllSessions` (sensitive-field stripping), `getSessionById` (owner vs. outsider vs. enrolled, enrollment found via parent track/course), `updateSession`, `deleteSession` (cascade), student add/remove, instructor/track/student filters                                                                                                                                                                                                                                                                                     |
| `announcement.test.js`                      | Announcement CRUD, role gating (instructor/admin can create; student cannot), public list/get, owner-vs-admin update/delete                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `events.test.js`                            | Event CRUD + role gating, public list/get, owner-vs-admin update/delete, RSVP + duplicate-RSVP rejection + cancel + `/my-events`                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `feed.test.js`                              | `GET /v1/feed` returns pinned announcements + upcoming events                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `contact.test.js`                           | Public contact form: success + every validation rejection                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `contactAdmin.test.js`                      | Admin list/view/update-status for contact submissions; auth and role gating on all three                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `activityLog.test.js`                       | Auth guards (401 unauthenticated, 403 non-admin except `/me`); `/me` self-scoping; admin list + `?action=` filter; per-user lookup + malformed-ID rejection; single get + 404; summary (totals/by-action/most-active-users, invalid-action rejection, `to`-before-`from` rejection); single delete + role rejection; prune (deletes-older-than, missing-param rejection, below-floor rejection); end-to-end proof that real signup/login/updateMe calls actually write a log entry                                                                |
| `services/activityLog.service.test.js`      | `logActivity` (creates entry, records target, never throws — returns null on Mongoose validation failure); `getAllActivityLogs` (pagination, `?action=` filter, populates user); `getActivityLogById` (+404); `getUserActivityLogs` (scoped to the ID argument, ignores a spoofed `?user=` query param); `getActivitySummary` (totals, by-action, most-active-users, date-range, single-action restriction); `deleteActivityLog` (+404); `pruneActivityLogs` (deletes only past the cutoff)                                                       |
| `dashboardStats.test.js`                    | Admin-only guards on every route (401 unauthenticated, 403 non-admin _and_ 403 instructor); `/live` (computed, not persisted); `POST /snapshot` (generates, validates `period`, upserts on repeat); `/latest` (404 before any snapshot exists, 200 after, missing-`period` rejection); `/trends` (ordered array, limit-ceiling rejection); list + `?period=` filter; get/delete by id (+404, +malformed-ID rejection); prune (deletes-older-than, missing-param rejection, below-floor rejection)                                                 |
| `services/dashboardStats.service.test.js`   | `getPeriodBounds` (daily/weekly/monthly boundaries incl. Sunday-anchored week and year-rollover month, bad period/date rejection); `generateSnapshot` (counts as of period end, submission-cutoff filtering, `avgCompletionRate` math incl. zero-enrollment exclusion, `mostActiveTrack` selection, upsert-not-duplicate); `getLiveStats`; `getAllSnapshots`/`getSnapshotById` (+404); `getLatestSnapshot` (+404 before any exist); `getTrends` (oldest-first, limited); `deleteSnapshot` (+404); `pruneSnapshots` (deletes only past the cutoff) |
| `reviews.test.js`                           | Course review creation (enrollment/duplicate/rating-range rejection), public listing, session-level review creation, review deletion (author, admin bypass, ownership rejection, 404)                                                                                                                                                                                                                                                                                                                                                             |
| `assignments.test.js`                       | Course + track assignment listing (`mySubmission`, enrollment gating, no submission-leakage), assignment create (course + session, ownership/role gated, untrusted-host rejection), update (ownership-gated, rejects empty body), delete (ownership-gated, admin bypass), submission (including untrusted-host rejection), resubmission clearing a grade, grading (ownership-gated, 404-before-submission)                                                                                                                                        |
| `weeklyTask.test.js`                        | Course-scoped weekly task creation (ownership, role, duplicate-week rejection) + per-student completion isolation                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `services/weeklyTask.service.test.js`       | `createWeeklyTask` (incl. duplicate-week), `getCourseWeeklyTasks`/`getTrackWeeklyTasks` (done-flags, enrollment gating, aggregation), `updateWeeklyTask` (preserves completions, week-clash rejection, orphaned-completion cleanup), `deleteWeeklyTask`, `setItemCompletion` (mark/unmark, 404, enrollment gating)                                                                                                                                                                                                                                |
| `updateMe.test.js`                          | The base64 photo regression (report item #4) + a normal-URL sanity check + garbage-input rejection + `enrolledTrack` presence (report item #8)                                                                                                                                                                                                                                                                                                                                                                                                    |
| `errorHandling.test.js`                     | CastError, duplicate-key (409), invalid JWT, and expired JWT — exercised through real routes                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `error.controller.test.js`                  | The global error handler in isolation: dev vs. production payload shape, `AppError` (operational) vs. generic errors, CastError/duplicate-key/ValidationError/`JsonWebTokenError`/`TokenExpiredError` mapping. (Its `'handles RateLimitError (if present)'` case is an empty stub — see note below.)                                                                                                                                                                                                                                              |
| `app.test.js`                               | `trust proxy` toggling, production request logging, 404 for unknown routes, the global rate limiter, CORS allow/deny, Swagger UI dev-only gating, `/health` (200 connected / 503 disconnected)                                                                                                                                                                                                                                                                                                                                                    |
| `APIFeatures.test.js`                       | `filter()` (operators, excluded reserved keys), `search()`, `sort()` default, `limitFields()`, `paginate()` metadata                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `utils/attachmentValidation.test.js`        | The Joi-level attachment validator: trusted hosts, non-HTTPS rejection, untrusted-host rejection, non-string items, >10-item rejection                                                                                                                                                                                                                                                                                                                                                                                                            |
| `validateAttachments.test.js`               | The Mongoose-level attachment validator: empty/undefined, trusted HTTPS hosts, non-HTTPS rejection, untrusted hosts, malformed URLs                                                                                                                                                                                                                                                                                                                                                                                                               |
| `utils/escapeHtml.test.js`                  | Escaping `&`, `<`, `>`, `"`, `'`, non-string input coercion, safe-character pass-through                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `utils/Email.test.js`                       | Constructor, `send`/error path, `sendWelcome`, `sendPasswordReset`, `sendEnrollmentConfirmation`, `sendSessionReminder`, transporter reuse                                                                                                                                                                                                                                                                                                                                                                                                        |
| `utils/logger.test.js`                      | Winston log level by environment, format config, console transport always present, file transports production-only, `asyncLocalStorage` export, request-ID injection (present and `"no-request-id"` fallback)                                                                                                                                                                                                                                                                                                                                     |
| `config/mailer.config.test.js`              | Transporter creation: `EMAIL_SERVICE` in production (with default `"SendGrid"`), host/port in development (with default port `2525`)                                                                                                                                                                                                                                                                                                                                                                                                              |

## What's deliberately NOT covered yet

- **`RateLimitError` handling is untested — and the one test for it is a stub with a misleading comment.** `error.controller.test.js`'s last case, `'handles RateLimitError (if present)'`, has an empty body; the comment above it claims "we removed this handler... it's dead code now." That comment is **wrong** — `handleRateLimitError()` is alive and wired up in `error.controller.js` (`if (error.name === 'RateLimitError') error = handleRateLimitError();`, mapping to a 429 `AppError`). So this isn't dead code, it's an untested branch with a stale comment pointing the wrong direction. Worth either writing a real assertion (throw an error with `name: 'RateLimitError'` at the test route and check for a 429) or at minimum correcting the stale comment so the next person doesn't get misled by it.
- **Email-sending is mocked, not asserted on.** Every test runs with `src/utils/Email` mocked as a no-op (see [Email is mocked globally](#email-is-mocked-globally)), so no test verifies that the _right_ email gets sent with the _right_ content at the _right_ trigger point (e.g. "does `enrollMe` actually call `sendEnrollmentConfirmation`?"). `utils/Email.test.js` tests the `Email` class's own behavior in isolation, but nothing asserts on its callers.
- **The `course`/`session` mutual-exclusivity validator on `Assignment`** — the model enforces exactly one of `course`/`session` must be set, but nothing tests that rejection directly.
- **`resourceAssignment.route.js` and `review.route.js` as generic factories** — their behavior is well covered indirectly (via `assignments.test.js` and `reviews.test.js` hitting the mounted `/courses/:id/...` and `/sessions/:id/...` paths), but there's no test asserting the factory itself produces identical behavior across all three resource types (track/course/session) in one place — track-level review/assignment creation in particular has less direct coverage than course/session.
- **Announcement audience targeting** — moot for now since `GET /v1/announcements` doesn't yet filter by `audience`/`targetTrack`/`targetCourse` (see README roadmap); once that filtering ships, it'll need tests.
- **Email verification self-service flow** — there's no send/verify-token endpoint yet (see README roadmap), so there's nothing to test.
- **`server.js`** itself (the `app.listen()` + DB-connect entry point) isn't imported by any test, by design — see [why server.js and src/app.js being separate matters](#why-serverjs-and-srcappjs-being-separate-matters). Its logic is intentionally thin enough that this is a reasonable line to draw.
- **`scripts/generateDashboardSnapshot.js`** has no direct test, same as `scripts/createAdmin.js` — both are thin CLI wrappers around an already-tested service function (`dashboardStatsService.generateSnapshot`), so the value of testing the wrapper itself (argv parsing, `mongoose.connect`/`disconnect`) is low relative to the cost of mocking a CLI entry point.
- **`logActivity` is only wired into auth, enrollment, and user-profile actions so far.** Track/course/session/event/announcement/assignment/weekly-task/review mutations don't yet call it, so there's nothing there for a test to assert on yet — this will need test coverage as each of those call sites gets wired up (see README roadmap).

## A good next test to write yourself

A solid next step is the assertion gap called out above: pick one flow that's supposed to trigger an email (e.g. `POST /v1/tracks/:id/students/:studentId/approve` → enrollment confirmation) and, in that one test file, replace the blanket mock with a `jest.spyOn` on the mocked `Email.prototype` method to assert it was called with the arguments you expect. That's a good template for locking down "did we actually wire this up" without abandoning the safety of never sending real mail.

## Writing your own tests: checklist

1. **Identify the endpoint or function** — is it public or protected? HTTP-level (`describe`/`request(app)`) or service/util-level (call the function directly)? If protected and HTTP-level, use `createTestUser` to get a token.
2. **Set up fixtures** — if the test needs data (e.g., a track, a course), create it directly with the model (see `tests/helpers/fixtures.js` for a reusable shape).
3. **Make the request/call** — use `request(app).method('/path')` and chain `.set()` for auth, `.send()` for body; or call the service/util function directly.
4. **Assert the result** — status code, response body shape, and/or database state.
5. **Clean up** — the database is cleared after each test automatically (`setupAfterEnv.js`), so you don't need to manually delete data.
6. **Update this file** — add a row to the [coverage table](#whats-covered-so-far) above so the next person (or you, in six months) doesn't have to re-derive what's tested by reading every file.
