# .github/workflows/keep-alive.yml
name: keep-alive
on:
  schedule:
    - cron: '*/14 * * * *' # every 14 minutes
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsS https://trosc.onrender.com/health || true
